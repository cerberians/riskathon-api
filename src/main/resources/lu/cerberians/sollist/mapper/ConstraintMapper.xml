<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lu.cerberians.sollist.mapper.ConstraintMapper">

    <insert id="create">
        CREATE (c:Constraint {id: #{constraint.id}})

        WITH c

        MATCH (a:Entity {id: #{constraint.aId}}), (b:Entity {id: #{constraint.bId}})
        CREATE (c)-[:FROM]->(a)
        CREATE (c)-[:TO]->(b)

        WITH c

        <choose>
            <when test="constraint.isWhitelist">
                MATCH (ct {id:"Whitelist"})
            </when>
            <otherwise>
                MATCH (ct {id:"Blacklist"})
            </otherwise>
        </choose>

        CREATE (c)-[:IS_TYPE]->(ct)
    </insert>

</mapper>